#include "main.h"

/**
 * print_percent - Prints the character %.
 *
 * This function uses the _putchar function to print the character represented
 * by the ASCII value 37, which is the '%' character.
 *
 * Return: Always returns 1.
 */
int print_percent(void)
{
	_putchar(37);
	return (1);
}
